# VSCode Implementation Prompt

新しい AGENT chat なら、最初に `Haruta_dev/prompts/agent-bootstrap.md` を使ってください。

そのうえで、以下の条件で進めてください。

- 目的:
- 対象ファイル:
- 制約:
- 完了条件:
- 確認方法:

注意:
- ルールの正本は `Haruta_dev/HarutaAGENTS.md` と `Haruta_dev/policies/`
- 既存ファイルは全文ではなく変更ブロックだけを提案する
- 変更ブロックには、何行目付近かと、どのコードの中、下、上に入れるかを示す
- 変更前コードは出さず、変更後コードだけを出す
- 提案は1変更ずつ順番に出す
- 解説は分かりやすく短く区切る
- コメントはコードの上に書く
- `コード理解タイム` のときは、理解用コメント追加を優先する
- 担当不明や他担当ファイルは、確認なしで変更候補にしない
- 命名は、短くて意味が分かるものを優先する
